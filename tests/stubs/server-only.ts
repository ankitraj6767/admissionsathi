/**
 * Test stub for the `server-only` package.
 * The real package throws when it is resolved outside the react-server
 * condition, which is exactly what happens when Vitest imports server
 * modules in a plain node environment.
 */
export { };
