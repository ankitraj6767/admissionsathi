node_modules
.next
out
build
dist
coverage
.env
.env.local
.env*.local
# Paste-ready production values for the Vercel dashboard. Real secrets — never
# commit. Not one of the filenames Next.js auto-loads, so it cannot leak into a
# local build.
.env.vercel
*.log
npm-debug.log*
.DS_Store
.vercel
.turbo
next-env.d.ts
tsconfig.tsbuildinfo
/test-results
/playwright-report
/playwright/.cache
/.playwright

# Locally uploaded media (STORAGE_PROVIDER=local). Never deployed — the
# serverless filesystem is ephemeral, so production must use Cloudinary.
/public/uploads
